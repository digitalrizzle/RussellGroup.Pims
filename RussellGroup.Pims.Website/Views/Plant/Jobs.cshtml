@model RussellGroup.Pims.DataAccess.Models.Plant

@{
    ViewBag.Title = "Jobs for " + Model.Description;
    var item = Model.PlantHires.FirstOrDefault();
}

<h2>@ViewBag.Title</h2>

<table class="table">
    <thead>
        <tr>
            <th></th>
            <th>
                @Html.DisplayNameFor(model => item.Job.XJobId)
            </th>
            <th>
                @Html.DisplayNameFor(model => item.Job.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => item.Job.WhenStarted)
            </th>
            <th>
                @Html.DisplayNameFor(model => item.Job.WhenEnded)
            </th>
            <th>
                @Html.DisplayNameFor(model => item.Job.ProjectManager)
            </th>
            <th></th>
        </tr>
    </thead>
</table>

@section scripts {
    <script>
        $(document).ready(function () {
            $('.table').dataTable({
                "bProcessing": true,
                "bServerSide": true,
                "bStateSave": true,
                "sAjaxSource": '@Url.Content("~/Plant/GetJobsDataTableResult?id=" + Model.Id)',
                "sPaginationType": 'full_numbers',
                "aoColumns": [
                    { "sWidth": "0%" },
                    { "sWidth": "10%" },
                    { "sWidth": "35%" },
                    { "sWidth": "10%" },
                    { "sWidth": "10%" },
                    { "sWidth": "30%" },
                    { "sWidth": "5%", "sClass": "right" }
                ],
                "aoColumnDefs": [
                    { "bVisible": false, "aTargets": [0] },
                    { "bSortable": false, "aTargets": [6] }
                ],
            });
        });
    </script>
}